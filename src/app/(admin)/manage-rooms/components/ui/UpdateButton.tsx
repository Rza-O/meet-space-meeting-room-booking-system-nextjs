import React from 'react';
import { CiEdit } from "react-icons/ci";

const UpdateButton = () => {
   return (
      <button className="btn btn-ghost btn-lg"><CiEdit /></button>
   );
};

export default UpdateButton;